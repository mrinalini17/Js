//singleton

//object literals

const JsUser={
    name: "Mrinalini",
    age: 21,
    location: "Bengalurur"
}

console.log(JsUser.name)
console.log(JsUser["name"])//better because full name can't be used with .

//values can be overwritten with =
//values can be freezed using Object.freeze(JsUser)


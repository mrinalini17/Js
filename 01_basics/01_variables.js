const accountId=144553 //can't be changed
let accountEmail="abcd@google.com" 
var accountPassword="12345" //not used anymore
accountCity="Kolkata" //not a good method

//console.log(accountId);
console.table([accountId, accountEmail, accountPassword, accountCity])  //every variable in tabular form, better than printing all the variables separately.

/*variable declared but no value= undefined */